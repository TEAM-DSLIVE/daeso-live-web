# 대소라이브 상세 설계

상태: MVP 기준 최종 설계

## 1. 서비스 경계

대소라이브는 대구소프트웨어마이스터고 학생을 위한 도담도담 AID 미니앱입니다.

```text
도담도담 AID
└─ 미니앱 실행·인증·배포

대소라이브
└─ 랜덤채팅·문의채팅 개발 및 운영
```

- 도담도담은 서비스 운영 주체가 아닙니다.
- 실제 서비스 운영 주체는 `대소라이브 운영팀`입니다.
- 팀명은 추후 결정합니다.

## 2. 사용자와 관리자

### 일반 사용자

- 도담도담 인증 학생
- 상대에게는 익명 닉네임만 표시
- 랜덤채팅과 본인 문의채팅 이용

### 관리자

- 문의채팅만 확인
- 사용자별 문의방 목록 확인
- 여러 문의방을 오가며 답변
- 랜덤채팅 내용에는 접근하지 않음

## 3. 전체 구조

```text
[도담도담 AID WebView]
        │ AID 토큰
        ▼
[대소라이브 프론트엔드]
        │
        ▼
[대소라이브 백엔드]
  ├─ 도담 인증·세션
  ├─ 랜덤 매칭
  ├─ 랜덤채팅 WebSocket
  ├─ 문의채팅 WebSocket
  └─ 문의 관리자 권한·저장소
```

랜덤채팅과 문의채팅은 같은 백엔드를 사용할 수 있지만, 방 종류·권한·저장 정책은 분리합니다.

## 4. AID 인증

```text
AID가 WebView 실행
→ 프론트엔드가 AID 토큰 수신
→ POST /auth/aid
→ 백엔드가 도담 OAuth 처리
→ 학교·계정 상태 확인
→ 대소라이브 세션 발급
```

- `Client Secret`은 서버 환경변수에만 보관
- 원본 도담 토큰은 로그·`localStorage`에 저장하지 않음
- 인증 성공 후 URL의 토큰 값을 제거
- 대구소프트웨어마이스터고 학생 여부는 백엔드에서 확인
- AID 인증 화면은 대소라이브 화면 설계 범위에서 제외

## 5. 일반 사용자 화면

```text
홈
→ 매칭 중
→ 랜덤채팅
→ 다음 상대 / 종료
```

### 화면 상태

- Dark/Light 대기
- Dark/Light 대화중
- Dark/Light 종료
- Dark/Light 홈
- Dark/Light 매칭 중
- Dark/Light 연결 오류
- Dark/Light 전송 실패

### 랜덤채팅 규칙

- 계정당 활성 랜덤채팅방 1개
- 대기 중인 다른 사용자와 무작위 매칭
- 자기 자신과 매칭하지 않음
- `다음 상대`를 누르면 현재 방 종료 후 매칭 화면으로 이동
- 사용자가 나가면 상대에게 종료 상태 표시
- 이미지·음성·영상·프로필·친구 기능 제외
- 메시지 최대 길이 제한 없음
- 매칭·요청 횟수 서버 제한 없음

## 6. 랜덤채팅 보안

```text
사용자 A 기기
  └─ 메시지 암호화
       ↓
대소라이브 WebSocket 서버
       ↓ 암호문 전달
사용자 B 기기
  └─ 메시지 복호화
```

- 메시지는 클라이언트에서 암호화
- 서버에는 메시지 평문을 저장하지 않음
- 서버에는 복호화 키를 저장하지 않음
- 채팅 기록 DB를 만들지 않음
- 메시지 평문을 로그에 남기지 않음
- 채팅 내용은 `localStorage`에도 저장하지 않음
- 서버가 확인할 수 있는 정보는 계정 ID·방 ID·접속 시간·연결 메타데이터임

암호화는 Web Crypto API 또는 검증된 라이브러리를 사용합니다. 자체 암호 알고리즘은 만들지 않습니다.

## 7. 사용자 문의채팅

랜덤채팅과 별도의 문의방입니다.

```text
설정
→ 문의하기
→ 대소라이브 운영팀과 1:1 문의
→ 설정으로 돌아가기
```

- 사용자 1명당 문의방 1개
- 문의방은 종료하지 않고 계속 유지
- 사용자가 화면을 나가도 문의방은 유지
- 새 문의는 기존 사용자 문의방에 이어서 기록
- 문의 메시지는 운영자 답변을 위해 저장 가능
- 문의 메시지는 랜덤채팅 암호화·비저장 정책과 별도 처리

### 사용자 문의 화면

- `문의가 접수됐어요` 시스템 안내
- 운영팀 메시지
- 사용자 메시지
- `문의 내용을 입력` 입력창
- `설정으로` 버튼

## 8. 관리자 문의 화면

관리자도 모바일 화면 기준으로 설계합니다.

### 문의 목록

목록은 문제 유형이 아니라 사용자별로 나눕니다.

```text
익명 사용자 A17                 방금 전
익명 사용자 B04                 3분 전
익명 사용자 C22                 12분 전
```

- 문제 내용은 목록에 표시하지 않음
- `종료됨`, `대화 중`, `읽음` 같은 상태 문구를 표시하지 않음
- 사용자 ID와 최근 활동 시간만 표시
- 문의 카드를 누르면 해당 사용자 문의방으로 이동

### 관리자 문의방

- 헤더에 익명 사용자 ID 표시
- 사용자·운영팀 메시지 표시
- `답변 입력` 입력창
- `문의 목록` 버튼
- 문의방을 종료하는 사용자 기능은 제공하지 않음

## 9. 문의 데이터 구조

### 문의방

```text
support_thread
├─ id
├─ userId
└─ lastMessageAt
```

### 문의 메시지

```text
support_message
├─ id
├─ threadId
├─ sender: USER | ADMIN
├─ content
└─ createdAt
```

문의 목록의 상태·읽음 기능은 현재 사용자 화면에 표시하지 않습니다. 필요할 때 백엔드 내부 필드로 추가합니다.

## 10. 최소 API

### 인증·랜덤채팅

```text
POST   /auth/aid
POST   /matching/join
DELETE /matching/queue
WS     /chat/random
```

### 문의채팅

```text
GET    /support/threads
GET    /support/threads/:userId
WS     /chat/support/:userId
POST   /support/messages
```

관리자 API는 관리자 권한을 별도로 확인합니다. 일반 사용자는 다른 사용자의 문의방에 접근할 수 없습니다.

## 11. 오류 처리

- 인증 만료: 재인증 안내
- 학교 계정 아님: 접근 거부
- 이미 채팅 중: 현재 방으로 이동
- 매칭 취소: 홈으로 이동
- 연결 끊김: 재연결 후 실패 시 오류 화면
- 랜덤채팅 전송 실패: 재전송 가능한 입력 상태 표시
- 문의채팅 연결 실패: 문의방 재연결 안내

## 12. Figma 구성

- 원본 섹션: `12:153` — 수정하지 않음
- 작업 복사본: `18:2` — `대소라이브 / 랜덤 채팅 UI / 확장안`
- 6열 그리드로 상태를 분류
- 사용자 화면과 관리자 화면을 별도 행으로 분리
- 관리자 화면도 일반 사용자 화면과 같은 핸드폰 프레임 사용
- Dark/Light 테마 제공
- Figma Prototype으로 주요 이동 흐름 연결

## 13. 제외 범위

- 이용약관 화면
- 차단·신고
- 이미지 전송
- 음성·영상 통화
- 친구·프로필
- 랜덤채팅 메시지 기록
- 메시지 최대 길이 제한
- 매칭·요청 횟수 제한

## 14. 구현 순서

1. 웹 UI를 Mock 데이터로 구현
2. AID OAuth 인증 연결
3. 랜덤 매칭 구현
4. 랜덤채팅 WebSocket·암호화 구현
5. 사용자 문의채팅 구현
6. 관리자 모바일 문의 목록·문의방 구현
7. 랜덤채팅과 문의채팅 권한·저장 분리 검증
8. AID WebView 및 교내 테스트

## 참고 문서

- [AID 빠른 시작](https://dodam-docs.b1nd.com/aid/getting-started)
- [AID 웹 통합](https://dodam-docs.b1nd.com/aid/web-integration)
- [AID OAuth 토큰 발급](https://dodam-docs.b1nd.com/aid/oauth-token)
- [DAuth 연동 가이드](https://dodam-docs.b1nd.com/dauth)
